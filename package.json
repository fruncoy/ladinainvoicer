{
  "name": "ladina-invoicer",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run prisma:generate --prefix server",
    "start": "npm run prisma:push --prefix server && node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
